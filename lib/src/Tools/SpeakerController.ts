// TODO: Use ffplay


import * as path from 'path'
import FFplay from './FFplay'

class SpeakerController {
    /*player = new Player()
    dir: string
    constructor (directory = '') {
        this.dir = directory
    }

    play (file: string, volume?: number) {
        if (file === '') throw new Error('file can\'t be empty')

        return new Promise((resolve) => {
            this.player.play(path.join(this.dir, file), {
                volume
            })
        })
    }*/
}

export default SpeakerController